# Audited TIMED_OUT set for the 'dispatch' suite (HARDENING.md, the audited-timeout
# bullet): line-less class,method,mutator keys, one per member. A timed-out mutant
# outside this set is a reviewer-stop — the ratchet cannot see a weakened covering
# assertion behind a timeout. Structural causes, and the lines they argue about, are
# in README.md. Measured under `qualityGate` 2026-07-28; re-observed history-free on
# 2026-09-14 (two full runs, identical) after the connection-hygiene rework, which
# retired the JdkQueryHandler lambda$handle$0 and process members with their code
# (README.md, "Retired members").
software.sava.http_servers.jdk.JdkQueryHandler, handle,           VoidMethodCallMutator # cause:liveness # quiet since the connection-hygiene rework, retained (README.md)
software.sava.http_servers.jdk.JdkController,   handle,           VoidMethodCallMutator # cause:liveness # line 79
software.sava.http_servers.jdk.JdkHttpServer,   start,            VoidMethodCallMutator # cause:liveness # line 15
